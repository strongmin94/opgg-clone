import styled from "styled-components";

export const StyleContainer = styled.div`
  position: relative;
  width: 100%;
  height: 100%;
`;

export const StyleRate = styled.p`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 14px;
  font-weight: bold;
  color: #555555;
`;
